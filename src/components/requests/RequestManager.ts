// class RequestManager {
//   constructor() {
//   }

//   // public getRequestByCreepId(creepId: string) {
//   //   let matchingRequests = _.filter(this.requests, (request) => request.id === id);
//   //   if (matchingRequests.length === 0) {
//   //     return null;
//   //   }

//   //   return matchingRequests[0];
//   // }

//   // public getRequestByTypeAndDistance(type: CreepType, maxDistance: number) {
//   //   let matchingRequests = _.filter(this.requests, (request) => request.type === type && request.distance <= maxDistance);
//   //   if (matchingRequests.length === 0) {
//   //     return null;
//   //   }

//   //   let matchingRequest = matchingRequests[0]
//   //   deleteRequest(matchingRequest.id);
//   //   return matchingRequests;
//   // }
// }
